# 06 工作台

外观对齐一代 BitX **桌面窗口**（你现在还在用的那个）：顶栏 BitX + 文件/编辑/选择/查看/转到/运行；左侧活动栏与资源管理器；中央编辑器；右侧 BitX 对话；底栏状态。禁止做成浏览器里的表单页。

活动栏：文件、搜索、扩展、终端、设置。中央 Monaco（打进旁路，不走 CDN）。右侧对话。底部状态栏。

只通过本机 RPC 访问磁盘与引擎（最高权限，不鉴权）。快捷键：Ctrl+O 开文件夹，Ctrl+P 快开，Ctrl+S 保存，Ctrl+` 终端，Ctrl+L 对话，Ctrl+Shift+X 市场。
